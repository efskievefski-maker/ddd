export const siteConfig = {
  // Master switch for all quote / order CTA buttons and quote form sections.
  // Set to `true` to re-enable every hidden quote/order CTA across the site.
  showQuoteButtons: false,
} as const
