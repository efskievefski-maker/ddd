export function trackConversion(source: string) {
  // Front-end-only conversion tracking stub.
  // Swap this for a real analytics call (e.g. analytics.track) later.
  console.log("[v0] conversion:", source)
}
